<h1><%= t(:profiles) %></h1>
<% @profiles.each do |profile|%>
    <div class="card card-body bg-light">
        <h3 style="color:black"><%=link_to "#{profile.name}", profile_path(profile), :class => 'btn btn-sm btn-outline-secondary' %></h3>
    </div><br>
<%end%>
